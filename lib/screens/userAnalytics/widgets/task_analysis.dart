import 'package:flutter/material.dart';

class TaskAnalysis extends StatelessWidget {
  const TaskAnalysis({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}